@model HomeViewModel

<partial name="_Carousel"/>

<h2>Os preferidos da semana </h2>
<h4>Tá com fome ???</h4>

<div class="row">
    @foreach (var snack in Model.SnacksFavorite)
    {
        <partial name="_SnackResume" for="@snack"/>
    }
</div>
